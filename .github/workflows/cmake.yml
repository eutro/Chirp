name: CMake

on:
  push:
    branches: [ master, ci ]
  pull_request:
    branches: [ master ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    strategy:
      matrix:
        running-on: [ubuntu-latest, macos-latest]
    
    runs-on: ${{matrix.running-on}}

    steps:
    - uses: actions/checkout@v2
    
    - name: Install LLVM and Clang
      uses: KyleMayes/install-llvm-action@v1.4.1
      with:
        version: "12.0"


    - name: Configure CMake
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}} -DCMAKE_RUNTIME_OUTPUT_DIRECTORY=targets src

    - name: Build
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

    - name: Upload Binaries
      uses: actions/upload-artifact@v2.2.4
      with:
        name: ${{matrix.running-on}}-binaries
        path: ${{github.workspace}}/build/bin/targets/
