defn cons(x, y) = // this is a comment
  let app(f) = f(x, y) in { app }

defn car(pair) =
  let f(x, y) = x in { pair(f) }

defn cdr(pair) =
  let f(x, y) = y in { pair(f) }

cdr(car(cons(cons(1, 1.0), "three")))
